// =  , ==, ===, !=, !==, >, <, >=, <=

console.log(1 == 1);


// =  is an assignment operator, it assigns the value on the right to the variable on the left.
// == is a comparison operator that checks for equality of values, it performs type coercion if necessary.
// === is a strict equality operator that checks for equality of both value and type, it does not perform type coercion.


// >,<, !=, <=,>=, 