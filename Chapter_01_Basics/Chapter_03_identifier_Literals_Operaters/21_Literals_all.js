let count = 42;
let negative_number = -5;
let binary = 0b1010; // Binary literal (10 in decimal)
let octal = 0o755; // Octal literal (493 in decimal)
let hexadecimal = 0xFF; // Hexadecimal literal (255 in decimal)

let isAvailable = false;
let isComplete = true;

let million = 1e6; // Exponential notation (1 million)
let smallNumber = 1e-6; // Exponential notation (0.000001)

let bigInt = 1234567890123456789012345678901234567890n; // BigInt literal
let stringLiteral = "Hello, World!";
let templateLiteral = `The value of count is ${count}`;
let nullValue = null;
let undefinedValue = undefined;

// single quotes
let singleQuoteString = 'This is a string with single quotes.';
// double quotes
let doubleQuoteString = "This is a string with double quotes.";
let backtickString = `This is a string with backticks.`;

let single = 'It\'s a single quote string with an escaped single quote.'
let single1= "let's go";         // SyntaxError: Unexpected identifier 'go' because of the unescaped single quote in "let's"


