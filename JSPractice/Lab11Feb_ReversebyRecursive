function reversebyrecursive(str){
    if (str === "") {
        return "";
    } else {
        return str.charAt(str.length - 1) + reversebyrecursive(str.slice(0, str.length - 1));
    }
}
console.log(reversebyrecursive("Hello"));

